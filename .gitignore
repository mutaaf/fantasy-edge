data/*.db
report.html
__pycache__/
*.pyc
.fantasy-edge/
*.env

# build + scratch
/tmp/
*.log
.DS_Store

# Xcode
apple/build/
*.xcuserdatad
xcuserdata/
# Which Apple developer team this particular Mac signs with: a local fact, not
# a project one. generate_project.py works it out from the installed
# provisioning profiles when this is absent (docs/DEVICE.md).
apple/signing.json

# replay captures: several hundred KB of ESPN payload per game, re-downloadable
# with `replay --capture`. The trimmed fixture under tests/ is the committed one.
data/replay/
.vercel

# Look-dev shots, scratch scenes and derived data
.work/
# Android client
clients/android/.gradle/
clients/android/.kotlin/
clients/android/build/
clients/android/app/build/
clients/android/local.properties
clients/android/.idea/
*.iml
