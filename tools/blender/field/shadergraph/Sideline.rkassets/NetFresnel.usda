#usda 1.0
(
    defaultPrim = "Root"
    metersPerUnit = 1
    upAxis = "Y"
)

def Xform "Root"
{
    # A net: its cord mask (uv0, the model's own repeats) times a view-angle
    # falloff. Seen flat-on from the stands the net faces the eye and nearly
    # vanishes; seen at a grazing angle, or up close where the cords resolve,
    # it reads. Unlit, like the fine cord it is under floodlights.
    #
    # Parameters (tokens: shaderGraph.materials.netFresnel):
    #   Mask     (asset)   cord coverage, grey
    #   Color    (color3)  cord colour
    #   Opacity  (float)   peak opacity
    #   Floor    (float)   fraction of Opacity kept when facing the eye
    #   Power    (float)   falloff exponent
    def Material "NetFresnel"
    {
        asset inputs:Mask = @./textures/placeholder_net.png@
        color3f inputs:Color = (0.8, 0.8, 0.78)
        float inputs:Opacity = 0.8
        float inputs:Floor = 0.08
        float inputs:Power = 1.6

        token outputs:mtlx:surface.connect = </Root/NetFresnel/Surface.outputs:out>

        def Shader "Surface"
        {
            uniform token info:id = "ND_realitykit_unlit_surfaceshader"
            color3f inputs:color.connect = </Root/NetFresnel.inputs:Color>
            float inputs:opacity.connect = </Root/NetFresnel/Final.outputs:out>
            bool inputs:applyPostProcessToneMap = 1
            bool inputs:hasPremultipliedAlpha = 0
            token outputs:out
        }

        def Shader "Cord"
        {
            uniform token info:id = "ND_image_float"
            asset inputs:file.connect = </Root/NetFresnel.inputs:Mask>
            float outputs:out
        }

        def Shader "Normal"
        {
            uniform token info:id = "ND_normal_vector3"
            string inputs:space = "world"
            float3 outputs:out
        }

        def Shader "View"
        {
            uniform token info:id = "ND_realitykit_viewdirection_vector3"
            float3 outputs:out
        }

        def Shader "Facing"
        {
            uniform token info:id = "ND_dotproduct_vector3"
            float3 inputs:in1.connect = </Root/NetFresnel/Normal.outputs:out>
            float3 inputs:in2.connect = </Root/NetFresnel/View.outputs:out>
            float outputs:out
        }

        def Shader "Abs"
        {
            uniform token info:id = "ND_absval_float"
            float inputs:in.connect = </Root/NetFresnel/Facing.outputs:out>
            float outputs:out
        }

        def Shader "Edge"
        {
            uniform token info:id = "ND_subtract_float"
            float inputs:in1 = 1
            float inputs:in2.connect = </Root/NetFresnel/Abs.outputs:out>
            float outputs:out
        }

        def Shader "Falloff"
        {
            uniform token info:id = "ND_power_float"
            float inputs:in1.connect = </Root/NetFresnel/Edge.outputs:out>
            float inputs:in2.connect = </Root/NetFresnel.inputs:Power>
            float outputs:out
        }

        def Shader "Lifted"
        {
            uniform token info:id = "ND_mix_float"
            float inputs:fg = 1
            float inputs:bg.connect = </Root/NetFresnel.inputs:Floor>
            float inputs:mix.connect = </Root/NetFresnel/Falloff.outputs:out>
            float outputs:out
        }

        def Shader "Scaled"
        {
            uniform token info:id = "ND_multiply_float"
            float inputs:in1.connect = </Root/NetFresnel/Lifted.outputs:out>
            float inputs:in2.connect = </Root/NetFresnel.inputs:Opacity>
            float outputs:out
        }

        def Shader "Final"
        {
            uniform token info:id = "ND_multiply_float"
            float inputs:in1.connect = </Root/NetFresnel/Scaled.outputs:out>
            float inputs:in2.connect = </Root/NetFresnel/Cord.outputs:out>
            float outputs:out
        }
    }

    def Sphere "Preview"
    {
        rel material:binding = </Root/NetFresnel>
        double radius = 0.1
    }
}
